import type { Viewport } from "next";

export const viewport: Viewport = {
  themeColor: "#654321",
};

export default function DefaultedRoutePatternPage() {
  return <h1>Notes defaulted children</h1>;
}
